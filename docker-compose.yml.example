version: '3.9'
services:
  bot:
    image: node:lts-alpine
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/telefood
    environment:
      - BOT_TOKEN:
      - CHAT_ID:
